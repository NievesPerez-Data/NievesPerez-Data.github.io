<header class="header">
<header class="header">
  <div class="logo-container">
    <div class="logo-box"></div>
    <h1 id="inicio">Nieves Pérez</h1>
  </div>
  
  <nav class="nav">
    <ul class="ul-nav">
      <li><a href="#inicio">Inicio</a></li>
      <li><a href="#proyectos">Proyectos</a></li>
      <li><a href="#skills">Habilidades</a></li>
      <li><a href="#estudios">Estudios</a></li>
      <li><a href="#sobremi">Sobre mí</a></li>
      <li><a href="mailto:mnieves.perez.alonso@gmail.com" class="contact-btn">Contacto</a></li>
    </ul>
  </nav>

  <div class="container-bnt-menu">
    <div class="btn-menu">
      <span class="linea-menu" id="linea1"></span>
      <span class="linea-menu" id="linea2"></span>
      <span class="linea-menu" id="linea3"></span>
    </div>
  </div>
  <div class="menu-drop"></div>
</header>

<style>
  :root {
    --emerald-light: #094035;
    --emerald-mid: #073028;
    --emerald-dark: #04201b;
    --pure-black: #000000;
  }

  .header {
    display: flex;
    flex-direction: row; /* Asegura horizontalidad */
    justify-content: space-between;
    align-items: center;
    height: 80px;
    background-color: var(--pure-black);
    padding: 0 40px;
    max-width: 1355px;
    margin: 0 auto;
    border-bottom: 1px solid var(--emerald-dark);
  }

  .logo-container {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .logo-box {
    width: 12px;
    height: 12px;
    background-color: var(--emerald-light);
    transform: rotate(45deg);
  }

  .header h1 {
    font-size: 1.2rem;
    color: #f6f6f9;
    letter-spacing: 1px;
    margin: 0;
  }

  /* --- ESTA ES LA PARTE QUE CORRIGE EL VERTICAL --- */
  .ul-nav {
    display: flex;
    flex-direction: row; /* FUERZA HORIZONTAL */
    list-style: none;
    gap: 25px;
    margin: 0;
    padding: 0;
    align-items: center;
  }

  .ul-nav a {
    text-decoration: none;
    color: #cbd5e1;
    font-size: 0.9rem;
    transition: 0.3s;
    position: relative;
    padding: 5px 0;
  }

  .ul-nav a:hover {
    color: #fff;
  }

  /* Subrayado esmeralda al pasar el ratón */
  .ul-nav a::after {
    content: "";
    position: absolute;
    width: 0;
    height: 2px;
    bottom: 0;
    left: 0;
    background-color: var(--emerald-light);
    transition: 0.3s;
  }

  .ul-nav a:hover::after {
    width: 100%;
  }

  .contact-btn {
    border: 1px solid var(--emerald-light);
    padding: 8px 16px !important;
    border-radius: 2px;
    background-color: var(--emerald-dark);
  }

  .contact-btn:hover {
    background-color: var(--emerald-mid);
  }

  /* Responsive: Aquí sí se vuelve vertical para el móvil */
  @media (max-width: 850px) {
    .container-bnt-menu { display: block; }
    .ul-nav {
      display: none; /* Se oculta el menú horizontal en móvil */
    }
    /* Si necesitas el menú móvil activado, añadiríamos la lógica de .menu-activado aquí */
  }
</style>

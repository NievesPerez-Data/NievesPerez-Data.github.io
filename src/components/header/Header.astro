<header class="header">
  <div class="logo-container">
    <div class="logo-symbol"><span class="dot"></span></div>
    <h1 class="name-logo">Nieves Pérez Alonso</h1>
  </div>
  
  <nav class="nav-container">
    <ul class="nav-links">
      <li><a href="#inicio">Inicio</a></li>
      <li><a href="#proyectos">Proyectos</a></li>
      <li><a href="#skills">Habilidades</a></li>
      <li><a href="#estudios">Estudios</a></li>
      <li><a href="#sobremi">Sobre mí</a></li>
    </ul>
    <a href="mailto:mnieves.perez.alonso@gmail.com" class="cta-contact">Contacto</a>
  </nav>
</header>

<style>
  /* Reset local para asegurar que no se rompa nada */
  .header {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 10000; /* Super alto para que no se esconda */
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: 90px;
    padding: 0 5%;
    background: rgba(0, 0, 0, 0.9);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(9, 64, 53, 0.4);
  }

  .logo-container { display: flex; align-items: center; gap: 12px; }
  .dot { 
    width: 10px; height: 10px; background: #0b5e4f; 
    border-radius: 50%; box-shadow: 0 0 15px rgba(11, 94, 79, 0.8);
    animation: pulse 2s infinite;
  }

  .name-logo {
    font-size: 1.5rem;
    font-weight: 700;
    letter-spacing: 1.5px;
    color: #ffffff;
    text-transform: uppercase;
    margin: 0;
    white-space: nowrap;
  }

  .nav-container { display: flex; align-items: center; gap: 35px; }
  .nav-links { display: flex; list-style: none; gap: 25px; margin: 0; padding: 0; }
  .nav-links a {
    text-decoration: none;
    color: #a0aec0;
    font-size: 0.85rem;
    font-weight: 500;
    transition: 0.3s;
  }
  .nav-links a:hover { color: #ffffff; }

  .cta-contact {
    text-decoration: none;
    color: #ffffff;
    font-size: 0.85rem;
    padding: 10px 20px;
    border: 1px solid #094035;
    background: rgba(9, 64, 53, 0.2);
    font-weight: 600;
  }

  @keyframes pulse {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.3); opacity: 0.6; }
  }

  @media (max-width: 950px) {
    .nav-links, .cta-contact { display: none; }
  }
</style>

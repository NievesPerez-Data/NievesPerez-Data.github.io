<header class="header">
  <div class="logo-container">
    <div class="logo-box"></div>
    <h1 id="inicio">Nieves Pérez Alonso</h1>
  </div>
  
  <nav class="nav">
    <ul class="ul-nav">
      <a href="#inicio"><li class="li-nav">Inicio</li></a>
      <a href="#proyectos"><li class="li-nav">Proyectos</li></a>
      <a href="#skills"><li class="li-nav">Habilidades</li></a>
      <a href="#estudios"><li class="li-nav">Estudios</li></a>
      <a href="#sobremi"><li class="li-nav">Sobre mí</li></a>
      <a href="mailto:mnieves.perez.alonso@gmail.com"><li class="li-nav contact-btn">Contacto</li></a>
    </ul>
  </nav>

  <div class="container-bnt-menu">
    <div class="btn-menu">
      <span class="linea-menu" id="linea1"></span>
      <span class="linea-menu" id="linea2"></span>
      <span class="linea-menu" id="linea3"></span>
    </div>
  </div>
  <div class="menu-drop"></div>
</header>

<style is:global>
  :root {
    --emerald-light: #094035;
    --emerald-mid: #073028;
    --emerald-dark: #04201b;
    --emerald-black: #02100d;
    --pure-black: #000000;
  }

  .header {
    position: relative;
    height: 90px;
    background-color: var(--pure-black);
    width: 100%;
    padding: 0 40px;
    color: #f6f6f9;
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1355px;
    margin: 0 auto;
    border-bottom: 1px solid var(--emerald-dark);
  }

  .logo-container {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .logo-box {
    width: 15px;
    height: 15px;
    background-color: var(--emerald-light);
    transform: rotate(45deg); /* Un rombo elegante en lugar de un círculo */
    border: 1px solid #0b5e4f;
  }

  .header h1 {
    font-size: 1.3rem;
    font-weight: 600;
    letter-spacing: 1px;
    color: #e2e8f0;
    text-transform: uppercase;
  }

  .li-nav {
    position: relative;
    list-style: none;
    transition: 0.3s;
    margin: 0 15px;
    padding: 5px 0;
    font-size: 0.95rem;
    color: #cbd5e1;
  }

  .li-nav::before {
    content: "";
    position: absolute;
    height: 2px;
    width: 0%;
    background-color: var(--emerald-light);
    bottom: 0;
    left: 0;
    transition: 0.4s;
  }

  .li-nav:hover::before {
    width: 100%;
  }

  .li-nav:hover {
    color: #fff;
    text-shadow: 0 0 8px rgba(9, 64, 53, 0.8);
  }

  .contact-btn {
    border: 1px solid var(--emerald-light);
    padding: 6px 18px !important;
    color: #fff !important;
    background-color: var(--emerald-black);
  }

  .contact-btn:hover {
    background-color: var(--emerald-mid);
    border-color: #0b5e4f;
  }

  /* Menú Móvil con tu paleta */
  @media (max-width: 720px) {
    .nav {
      background: var(--emerald-black);
      border-left: 1px solid var(--emerald-mid);
    }
    .linea-menu {
      background-color: var(--emerald-light) !important;
    }
    .li-nav:hover {
      background-color: var(--emerald-dark);
    }
  }
</style>
